// Supprimme pas cette ligne
const Test = require("@codewars/test-compat");

function opposite(number) {
  return 42;
}

describe("Tests", () => {
  it("test", () => {
    Test.assertEquals(opposite(1), -1);
  });
});


